# Persistent state for the Wolf streaming host (see
# /usr/share/containers/systemd/wolf.container). Mounted into the container at
# /etc/wolf, which is where Wolf writes config.toml and its pairing keys.
d /var/lib/wolf 0700 root root -
